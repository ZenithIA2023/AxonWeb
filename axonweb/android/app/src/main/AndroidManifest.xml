<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Deep link do OAuth: o Google autentica no navegador do sistema e
                o backend redireciona para com.axon.app://..., que o Android
                entrega de volta a esta activity (launchMode singleTask reusa a
                instância já aberta em vez de empilhar outra).
                O esquema precisa casar com MOBILE_SCHEME no backend e com
                DEEP_LINK_SCHEME em src/lib/nativeAuth.ts.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.axon.app" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Voz do Axon (Fase 4). O Capacitor/WebView pede as duas juntas no
        primeiro getUserMedia() — faltando MODIFY_AUDIO_SETTINGS, o Android
        recusa em silêncio mesmo com RECORD_AUDIO presente, e o app parece
        travado sem nenhum diagnóstico. Não precisa de plugin nativo: o
        BridgeWebChromeClient do Capacitor já dispara o diálogo do sistema
        sozinho no primeiro uso do microfone pela WebView.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!--
        required="false": um aparelho sem microfone físico continua podendo
        instalar o app (só a voz fica indisponível) — sem isso a Play Store
        esconderia o Axon desses aparelhos por completo.
    -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
</manifest>
